{% macro render(title, classes) %}

<section class='card {{classes}}'>

  <div class='card-header'>
    <h3>{{title}}</h3>
  </div>

  <div class='card-container'>
    {{ caller() }}
  </div>

</section>

{% endmacro %}