<!DOCTYPE html>

<html lang="en">

<head>

  <link rel="stylesheet" href="/styles.css">

  <title>Alunos</title>

</head>

<body style="background: url(https://source.unsplash.com/collection/620529/800x800) no-repeat center center / cover">

  <header>

    <div class='links'>

      <a href="/">Sobre</a>
      <a href="/professores">Professores</a>
      <a class='selecionado' href="/alunos">Alunos</a>
      
    </div>

  </header>

  <div class='card'>

    <section class='avatar' 
    style="border-radius: 20px 0 0 20px; background: url({{ aluno.avatar_url }}) no-repeat center center / cover"></section>
    
    <section class='detalhes'>
    
      <h3>Detalhes</h3>

      <div class='item'>
        <div>Nome</div>
        <div>{{ aluno.nome }}</div>
      </div>

      <div class='item'>
        <div>Idade</div>
        <div>{{ aluno.nascimento }}</div>
      </div>

      <div class='item'>
        <div>Email</div>
        <div>{{ aluno.email }}</div>
      </div>

      <div class='item'>
        <div>Ano escolar</div>
        <div>{{ "5° ano do Ensino Fundamental" if aluno.ano_escolar == "5F" else 
          ("6° ano do Ensino Fundamental" if aluno.ano_escolar == "6F" else 
          ("7° ano do Ensino Fundamental" if aluno.ano_escolar == "7F" else 
          ("8° ano do Ensino Fundamental" if aluno.ano_escolar == "8F" else 
          ("9° ano do Ensino Fundamental" if aluno.ano_escolar == "9F" else 
          ("1° ano do Ensino Médio" if aluno.ano_escolar == "1M" else 
          ("2° ano do Ensino Médio" if aluno.ano_escolar == "2M" else "3° ano do Ensino Médio")))))) }}
        </div>
      </div>

      <div class='item'>
        <div>Carga horária semanal</div>
        <div>{{ aluno.carga_horaria }}</div>
      </div>

      <div class='item'>
        <div>Desde</div>
        <div>{{ aluno.data_criacao }}</div>
      </div>

      <div class='item'>
        <div>Aluno de</div>
        <div>{{ aluno.professor_nome }}</div>
      </div>

      <a href='/alunos/{{ aluno.id }}/edit' class='button'>Editar</a>

    </section>
    
  </div>
  
</body>

</html>